void setup(){
  size(200,200);
}
void draw(){
  noStroke();
  colorMode(HSB, 360, 100, 100);
  fill(mouseX, mouseY, mouseX);
  //this fill function says that the HSB of the color depends on the mouse's location//
  rect(mouseX, mouseY, 30,30);
}
